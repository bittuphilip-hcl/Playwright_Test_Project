P**re-requiste**: Latest version of Node-js and npm should be installed in your local machine.

1. To install all the dependencies from package.json file run the command "**npm install**"
2. To Execute the scripts, run the command
**npx cucumber-js**   or **npm run test**
